$VAR1 = {
          'T_AN_INTERRUPTIB' => 'An interruptible sound means that a caller can press keys while the sound is playing. If the key corresponds to an option in your IVR, the sound will stop playing and those actions will be triggered. If a sound is not interruptible, when a caller presses keys while the sound is playing, the system ignores them and the sound will continue to play.',
          '__INCLUDES' => [
                            'common/incl_help_header',
                            'common/incl_help_footer'
                          ]
        };
