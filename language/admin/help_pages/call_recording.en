$VAR1 = {
          'T__WHEN_A_USER_R' => '. When a user records a call (his own call or another 
person\\\'s), those recordings are shown here. These are the same recordings that the 
user has in his Mailbox, so if you or the user deletes one of these recordings,
it is no longer available in either area. However, if the user forwarded the
recording to another Switchvox user, that version is unchanged.',
          '__INCLUDES' => [
                            'common/incl_help_header',
                            'common/incl_help_footer'
                          ],
          'T_THERE_IS_ONE_D' => 'There is one default call recording rule that Switchvox automatically follows, called',
          'T_USERINITIATED' => 'user-initiated recordings',
          'T_THIS_LETS_YOU2' => 'This lets you set up rules for recording calls. Any calls that match these 
rules are recorded and listed on this page. You can view, listen to, and download recorded calls.'
        };
